# Echo MCP server (skeleton)

This is a placeholder to show where an MCP server example would live.

In a production repo, this would include:
- server implementation
- manifest (AEM/APM)
- container build recipe (to support sandboxing)
- signing + provenance via CI

